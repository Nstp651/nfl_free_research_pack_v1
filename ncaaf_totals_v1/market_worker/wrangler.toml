name = "ncaaf-totals-market-v1"
main = "index.js"
compatibility_date = "2026-09-05"
workers_dev = true
preview_urls = false

# Runtime secret required in Cloudflare (never commit the value):
#   ODDS_API_KEY
# Cloudflare Git Builds deploy command installs the encrypted build secret
# into the Worker runtime secret after each deployment.
